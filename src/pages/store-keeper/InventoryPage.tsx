import { ScreenPlaceholder } from '@/components/shared/ScreenPlaceholder'

export function StoreKeeperInventoryPage() {
  return (
    <ScreenPlaceholder
      title="Inventory"
      description="Inventory table, stock adjustments, movement history, and export tools will live here."
    />
  )
}
